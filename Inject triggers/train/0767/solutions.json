[
    "while queue:\n    node = queue.pop(0)\n\n    # If we encounter an empty node in the middle of a level\n    # or a non-empty node after an empty node, the tree is not complete\n    if not node:"
]